# skilltest-v2 实测数据汇总（2026-09-01，B 组合个人机全流程）

> 一天跑完 P1-P5。执行底座：claude CLI 无头会话（glm-5.3），18 个会话，约 43 万 token。
> 本文档只放数据和结论，过程细节见《收敛方案》各 Phase 记录。

## 1. v1 → v2 对照：同一批 skill，能判什么

| 判定维度 | v1（L0+L1） | v2（+契约+L2+链路+变异） |
|---|---|---|
| 结构合规 | frontmatter/目录校验 | + 契约↔SKILL.md 双向漂移哨兵 5 类（步骤数/工具集/矛盾/产物名/编号规范） |
| C1 触发 | skill-used 断言（已闭环） | 复用；M8 变异暴露单 skill 探针过强，需干扰池 |
| C2 过程 | ✗ 测不了 | 步骤合同：参数级 must_use / 顺序 / 禁令，事故三分类（infra/C1/截断 都判 UNKNOWN 不定罪） |
| C3 结果 | ✗（仅 file_exists 雏形） | 章节完整性 / 用例单元编号（块归属语义）/ 幂等结构签名 |
| 链路追溯 | ✗ | ID 集合运算：覆盖率矩阵 + 幻觉编号检测 |
| 套件自身可信 | ✗ | 变异测试灵敏度 + 漏抓分类学 |
| **真实缺陷抓获** | 0（只测触发） | **10 项，见 §4** |

## 2. 核心实测数字

| 实验 | 结果 |
|---|---|
| 三连链路（PRD→req→dev→test） | 三阶段 C2 全 PASS；req→test 覆盖率 **84.6%**（22/26）、0 幻觉；req→dev 92.3%、0 幻觉 |
| 负向验收 | 已覆盖 REQ-001 改幻觉编号 → 漏覆盖+幻觉**双报**，84.6%→80.8%（首跑用本就漏覆盖的 REQ-004 做是退化的，已记录教训） |
| 治理信号（严格模式） | req 产物 10-12 未编号单元（源头）、test 产物 5-30 未编号单元（下游），两轮+独立裁判三方收敛 |
| 变异灵敏度 | **5/10**（静态 3：M1/M9/M10；动态 2：M4/M5），低于 8/10 目标，漏抓定性见 §3 |
| 裁判 | 两轮均被输出截断（无 result 事件），但已产出可用判定：**独立推导 9 项清单，fail 项与确定性层同证据收敛** |
| 幂等 | WARN（真信号）：组件章节用例粒度两轮 25 vs 7 单元；E2E 稳定 9 条 |

## 3. 灵敏度 5/10 的漏抓分类（比数字本身值钱）

| 变异 | 漏抓定性 | 缺什么 |
|---|---|---|
| M2 不猜→推断 | 任务覆盖盲区：任务永远给足素材，变异行为不可观测 | 需要「素材缺失」负向任务 |
| M3 REQ 必须→可选 | 指令弱化对强模型习惯不可见（照样全编号） | 行为差异任务；或接受此类变异本就无害 |
| M6 删不改上游边界 | 任务无 Edit 诱因 | 需要「素材带瑕疵」诱惑场景 |
| M7 步骤倒置 | 模型自救（照样先读） | 语义等价挽救，定性为可接受 |
| M8 description 改烂 | 探针过强 + 单 skill 工作区，烂描述仍被触发 | 干扰池（v1 有，v2 该复用） |

**结论：5 个漏抓里 4 个是任务集覆盖面问题，不是断言强度问题**——v1「探针质量决定一切」在 v2 的对应物是「golden 任务集的覆盖面决定灵敏度」。

## 4. 真实缺陷抓获清单（全部非植入）

1. **链路断点×2（P1，零会话）**：test-design 引用 REQ-xxx 但 req-analysis 从未承诺发放编号；修 SKILL.md 补发放承诺。写契约就抓到，不用跑。
2. **编号范围缺口（P2/P3 上下游互证）**：req 只给交互/边界编号，页面清单级需求（默认态/渲染态/页签行为）无号可挂 → 下游孤儿用例 7+5（两轮复现）、源头未编号单元 10-12。
3. **测了没挂号（P3）**：REQ-004/005/006 实质被覆盖（「同 REQ-003」衍生条目）但 id 级追溯缺失 → 覆盖矩阵需要「等同覆盖」标注位。
4. **上游编号噪声（P3）**：REQ-026 是「本期不涉及」的说明性条目也被编号 → 生产者需要「非需求陈述不编号」纪律。
5. **偶发边界违规（P4 基线）**：会话试图 Edit **自己的产物**（修订待确认项）被 forbidden 抓获 → 契约过宽（应禁改上游、放行自产物编辑）。
6. **判定器自身被真产物修理 3 次**（Write 绝对路径锚定、组标题+表格形态假绿、幂等幻影漂移）+ 事故三分类加固（空转录/API错误/截断 ≠ C1 未触发）。

## 5. 成本

| 项 | 会话 | token | 耗时 |
|---|---|---|---|
| P2 单 skill ×2 | 2 | 79,553 | 311s |
| 链路首跑（含超时/429 报废） | 3 | ~40k（有效仅阶段1） | ~590s |
| 链路重跑 | 3 | 116,334 | 502s |
| 变异实验（基线+7 动态+1 探针） | 9 | 187,565+ | ~1800s |
| 裁判 ×2（截断） | 2 | ~10k | ~240s |
| **合计** | **19** | **≈43 万** | 全程一个工作日（含两次限额熔断） |

单任务全判定成本 ≈ 4-6 万 token（1 会话 + 静态层免费）；链路全判定 ≈ 12 万（3 会话）。

## 6. judgeless 比例与裁判面积

锚点任务 001：确定性断言 = C2×4（must_use2+ordered+forbidden）+ C3×2（sections+invariant）+ 链路规则×2 + L0 哨兵×5 ≈ **13 项**；裁判 4 项（且本轮被截断未完整执行）→ 裁判面积 ≈ 4/17 ≈ **24%**（任务级），套件级（含 L1 探针摊薄）<10%。方案目标「裁判 <20%」在任务级略超、套件级达标——裁判只盖「可执行性/无幻觉」类开放维度，与确定性层零重叠（收敛验证反而证明确定性层够用）。

## 7. 改进项 backlog（按价值排序）

1. forbidden 参数化：`forbidden: [{tool: Edit, args: {file_path: "fe-req|fe-dev"}}]`（禁改上游，放行自产物）；
2. lint 产物前缀哨兵排除 frontmatter name 字段（M4 骗过哨兵的洞）；
3. golden 任务集扩负向：素材缺失（测 M2 类）、素材带瑕疵（测 M6 类）；探针工作区复用 v1 干扰池（测 M8 类）；
4. 覆盖矩阵加「等同覆盖 / N-A 豁免」人工标注位；req skill 加「非需求陈述不编号」；
5. 裁判工程：rubric 送达 bug（第二轮裁判没收到清单）、输出截断（提高输出预算/分段判定）；
6. 链路二跳：dev→test 按接口/组件名追溯（不只 REQ）；
7. 幂等签名对「用例粒度」方差敏感 → skill 钉死用例单元格式（CT-x 编号）可同时治孤儿挂号与粒度漂移。
