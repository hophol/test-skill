# run artifacts (reports, per-run workspaces, logs) - regenerate with cc-eval run
cc-eval/out/
skill-eval/out/

# dependencies / machine-specific links
node_modules/
skill-eval/plugin/node_modules/

# local noise
*.log
.DS_Store
Thumbs.db
