# 发散：如何自动判定「skill 能工作、产出正确结果」

> skilltest-v2 第一阶段（发散）。v1 已闭环 C1 触发判定（探针 + skill-used 断言）；
> 本篇只谈 v1 刻意绕开的部分：**skill 被触发之后，怎么自动知道它做对了**。
> 原则不变：能确定性断言的绝不用裁判——本篇的任务是把「必须用裁判」的领地缩小到什么程度。

---

## 0. 先把命题拆干净：三个独立命题，两种独立失败

「skill 能工作、产出正确结果」不是一个命题，是三个：

| 命题 | 含义 | v1 状态 |
|---|---|---|
| C1 触发 | 该触发时 Skill 工具被调用 | ✅ 已闭环 |
| C2 过程正确 | agent 真的按 SKILL.md 说的做了（步骤/工具/顺序/禁令） | 未测 |
| C3 结果正确 | 最终产物满足任务的真实要求 | 未测（v1 只测了副作用的确定性断言雏形） |

**C2、C3 会独立失败**，混在一起测会误诊：

- C2 ✅ C3 ❌：步骤全走对了，但结果错（fixture 本身脏、模型执行走样）→ 修 skill 内容；
- C2 ❌ C3 ✅：结果对了，但 skill 步骤根本没走——裸模型本来就会，**skill 没有增量价值** → 这不是测试问题，是 skill 该不该存在的问题（见 M3）。

所以 v2 的套件必须 C2/C3 分开断言、分开报告。

## 1. 总洞察：可测性是 skill 输出契约的属性，不是测试工具的属性

「这个 skill 的产出没法自动判」绝大多数等价于「这个 skill 的产出没有被规约」。
测试自动化做不下去，根因往往在上游资产形态。推论：

> **skill 测试自动化的前一半工程，是 skill 资产的规约化**——每个 skill 随附一份可机读的
> 输出契约（schema / 模板 / 追溯规则 / 步骤合同），契约本身就是判据来源。

这一步做不做，决定后面所有机制的成本量级。这也是 v2 与 v1 最大的分野：
v1 改造的是**测试工具**，v2 要碰**被测资产的形态**。

## 2. 判定依据光谱

```
确定性代码断言 ────────────────────► 自由文本 LLM 裁判
  可靠/免费/可进门禁/可解释        覆盖广/贵/flaky/要纪律
```

v1 的经验（事件不查文本）本质是把 C1 推到了最左端。
v2 的全部候选机制，回答的都是同一个问题：**某一类 skill 的 C2/C3 能被推向光谱左端多远。**

---

## 3. 候选机制清单

### M1 输出契约 + 金标断言（经典 golden）
- **机制**：skill 声明输出 schema；任务带人工/脚本产出的金标文件；diff 判定。
- **适用**：输出结构化且答案唯一（数据提取、格式转换、代码生成）。
- **不适用**：开放性文档（措辞千变）；金标维护是持续成本。
- **确定性**：100%。**金标从哪来**是瓶颈 → 由 M8 解决一部分。

### M2 属性测试 & 蜕蜕测试（judgeless 的新主力）★
- **机制**：不对答案，对**不变量**。四类：
  - 守恒：源表行数 == 输出行数；金额分项合计 == 总额；
  - 可逆：翻译→回译语义稳定；生成 YAML → 能被 parser 解析回来；
  - 幂等：同任务跑两遍，结构等价；
  - 蜕变（metamorphic）：输入受控变换后输出必须发生可预测变化——
    需求加一条 → 测试设计用例数 ≥ 原数+1；删一列 → 输出 CSV 该列消失且其余不动。
- **价值**：完全绕开金标、完全确定性、天然抗「措辞不同」的假阴。
- **边界**：蜕变关系设计错了会**假绿**（不变量本身不成立），每条不变量要有一次人工论证。
- **成熟度**：2024-2026 已成 LLM 测试研究热点（[LLM 蜕变测试框架](https://dl.acm.org/doi/10.1145/3787120.3787123)、
  [Anthropic：用 PBT 找 bug](https://www.anthropic.com/research/property-based-testing)、
  [多 agent 挖掘蜕变关系](https://arxiv.org/html/2605.28321v1)）。

### M3 差分测试（三个方向，各自回答不同问题）★
- **vs 脚本参考实现**：一类任务能用确定性脚本产出正确答案的，批量造金标，judgeless 回归；
- **vs 旧版本**：回归不再全量判定——只对「新旧输出 diff」做小面积审读，成本从 O(全文) 降到 O(diff)；
- **vs 裸模型（无 skill A/B）**：同一任务「带 skill / 不带 skill」各跑 n 次，比较 C3 通过率——
  **测的是 skill 的增量价值**。C3 过但无增量 = skill 是装饰品。对 100+ skill 的治理
  （哪些该合并、该砍）这是唯一客观指标，v1 完全没有这个维度。
- **成熟度**：SE 领域差分测试成熟（[DiffSpec](https://arxiv.org/html/2410.04249v3)、
  [Galápagos N-version](https://dl.acm.org/doi/10.1145/3785363)）。

### M4 步骤合同（转录断言）——C2 的主力 ★
- **机制**：把 SKILL.md 里的步骤/checklist 编译成可机读合同：必用工具、调用顺序、
  参数约束、禁用动作（如「不许直接猜，必须先读文件」）；从会话转录确定性断言。
- **关键设计点**：合同是维护者另写一份 YAML，还是从 SKILL.md checklist 自动抽取？
  （另写 = 双份维护会漂移；自动抽取 = 抽取器本身要可信——v1 的转录解析资产可复用。）
- **成熟度**：标准实践（[Anthropic evals 指南](https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents)、
  [MLflow 评 Claude Code skills](https://mlflow.org/blog/evaluating-skills-mlflow/) 都以转录为判据）。

### M5 下游消费者校验 + 链路不变量——文档类 skill 的杀手锏 ★★
- **机制**：文档「正确」太难判，换成「**能被下一个消费者无损消费**」：
  - 生成的 YAML 能解析、代码能编译、测试能跑通——**消费者程序就是裁判**；
  - **跨阶段追溯不变量**（全确定性，直击需求→开发→测试主域）：
    需求文档每个 REQ-ID 在测试设计中 ≥1 条用例覆盖；
    开发任务 ↔ 需求双向可追溯；测试设计引用的字段在需求里真实存在（防幻觉字段）。
- **价值**：单看一份文档好坏没有稳定判据，但**流水线两端的约束关系**是硬的。
  这一步不需要裁判，只需要把「下游需要什么」写成机读契约。
- **边界**：需要跨阶段契约约定（谁消费谁的产物、ID 规范）——是组织协调成本，不是技术成本。

### M6 环境副作用断言
- **机制**：skill 操作外部系统（发请求、写库）时，用 mock server / 记录回放，
  断言收到的调用序列与参数。产物正确性 = 副作用正确性。
- **适用**：动作型 skill。**确定性**：高（回放固定了环境）。

### M7 对抗验证会话（裁判的收敛形态，不是通用裁判）
- **机制**：不做「打分裁判」。专职验证 agent 拿到 任务+产物+skill 全文，
  提示词导向**证伪**：「找出这个产物不满足任务要求的证据」，逐项二值 + 引用证据。
  可多镜头并行（正确性 / 幻觉 / 与 skill 声明不符）投票。
- **定位**：只盖 M1–M5 盖不住的残差（开放性质量维度），面积越小越成功。
- **成本**：每次判定 1-N 次模型调用；纪律沿用 v1（逐项二值、禁整体打分）。

### M8 评测资产的生产自动化
- **机制**：100+ skill × 每个 5-15 任务，手写不可持续。三个来源：
  fixture 生成器（模板 + 参数化批量产任务）；对抗性素材变异（制造脏数据边界 case）；
  生产回流（见 M10）。
- **定位**：不是判定机制，是**规模化前提**。任务集是资产（v1 结论），资产要有生产线。

### M9 变异测试（测的是测试集本身）★
- **机制**：故意弄坏 skill——删一步、改一个阈值、换一个文件引用、把「必须先读文件」改成「可以直接猜」——
  套件必须在对应断言上报警。能被抓住的变异比例 = 套件灵敏度。
- **价值**：100+ skill 多人维护，最现实的事故不是 skill 写错，是**被人随手改坏而无人察觉**。
  M9 直接回答「改坏了会不会被发现」，这也是唯一能验证「v2 套件本身可信」的手段。
- **成本**：m 个变异体 × 套件一轮。可抽检（每次只测几个变异体）。
- **副产物**：变异体 = 天然的回归练习册 / 新人培训素材。

### M10 生产回流与漂移监测
- **机制**：真实使用中，用户大幅改写/否决 skill 产物 = 一条现场挂掉的测试用例，
  采样回流进套件；模型或环境变更时，用锚点集（少量人工金标）测整体漂移。
- **定位**：让系统能自己变好（v3 的回流机制在 L2 的落地形态）。

---

## 4. 按 skill 输出类型分型（机制组合矩阵）

| 输出类型 | C2 过程 | C3 结果 | 裁判残余 |
|---|---|---|---|
| 结构化数据（提取/转换） | M4 | M1 + M2 + M3(脚本金标) | ≈0 |
| **文档类（需求/开发/测试设计——主域）** | M4 | **M5(链路不变量) + M2(守恒/追溯) + M3(anchor集)** | M7 小面积 |
| 代码/脚本类 | M4 | M5(编译/测试跑) + M1 | 幂等/风格 |
| 动作环境类（调外部系统） | M4 + M6 | M6 + M1 | 少 |

规律：**输出越开放，C3 越依赖「约束关系」（M5/M2）而不是「内容比对」（M1）**。

## 5. 收敛预打包（三个候选主线）

| 组合 | 内容 | 特点 |
|---|---|---|
| A 保守 | M1 + M4 + M7 | v1 的自然延伸，最快落地，裁判面积大 |
| B 主推 | M4 + M5 + M2 + M7(小面积) + M9(抽检) | judgeless 优先，配套契约规约，直击文档类主域 |
| C 激进 | B + M3(全三个方向) + M8 + M10 | 奔 100+ skill 治理去：含增量价值判定与生产回流 |

## 6. 边界与风险（实事求是）

- M5 依赖跨阶段契约——组织成本大于技术成本，方案里要给它一条单独的推进线；
- M2 蜕变关系本身错 → 假绿，每条不变量上线前要一次人工论证；
- M9 成本 = 变异体数 × 套件一轮，只抽检不全量；
- M3「vs 裸模型」会消耗双倍 token，只对存疑 skill 跑；
- 明确不测：主观美感、「agent 是否整体变聪明」（沿用 v1 边界）。

## 7. 与 v1 的关系

- v1 资产全复用：转录解析、探针框架、报告/门禁管线是 v2 的执行底座；
- v1 的 L1（触发）继续存在，v2 加 L2（C2+C3），共用同一套 runner/报告；
- 分歧点只在第 1 节：v2 主张**碰被测资产形态**（要求 skill 随附契约），这超出了纯测试工具的边界，
  是方案里要明确向维护者提要求的部分。

---

*检索依据：蜕变/属性测试（[ACM 框架](https://dl.acm.org/doi/10.1145/3787120.3787123)、[Anthropic PBT](https://www.anthropic.com/research/property-based-testing)、[arXiv 多agent蜕变](https://arxiv.org/html/2605.28321v1)）、差分测试（[DiffSpec](https://arxiv.org/html/2410.04249v3)、[Galápagos](https://dl.acm.org/doi/10.1145/3785363)）、转录断言（[Anthropic evals](https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents)、[MLflow skills](https://mlflow.org/blog/evaluating-skills-mlflow/)）。检索日 2026-09-01。*
